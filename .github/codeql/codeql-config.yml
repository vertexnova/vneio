# ----------------------------------------------------------------------
# CodeQL configuration for VneIo
# Excludes third-party code under deps/external from analysis/reporting
# so CodeQL findings in e.g. googletest do not appear in code scanning.
# ----------------------------------------------------------------------

name: vneio CodeQL config

paths-ignore:
  - "deps/external/**"
